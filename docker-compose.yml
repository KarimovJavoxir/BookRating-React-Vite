services:
  frontend:
    image: edwardkenway/book-rating-frontend
    build:
      context: .
      dockerfile: Dockerfile
      network: host
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-https://api.kitobreytingi.uz}
